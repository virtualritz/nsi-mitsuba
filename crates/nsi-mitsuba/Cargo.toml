[package]
name = "nsi-mitsuba"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0 OR Zlib"
description = "Mitsuba 3 backend for the Nodal Scene Interface – ɴsɪ."
repository = "https://github.com/virtualritz/nsi-mitsuba/"

[dependencies]
# The renderer-agnostic IR now lives in the nsi workspace, beside the
# trait it implements. Alias it as `nsi_ir` if the full name grates.
nsi-intermediate = { git = "https://github.com/virtualritz/nsi.git" }
